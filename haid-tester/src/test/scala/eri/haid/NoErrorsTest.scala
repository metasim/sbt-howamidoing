package eri.haid

class NoErrorsTest extends HaidTestSpec {
    describe("something that has no errors") {
        it("will not have an error 1") {
            assert(true)
        }

        it("will not have an error 2") {
            assert(true)
        }

        it("will not have an error 3") {
            assert(true)
        }

        it("will not have an error 4") {
            assert(true)
        }

        it("will not have an error 5") {
            assert(true)
        }

        it("will not have an error 6") {
            assert(true)
        }
    }
}